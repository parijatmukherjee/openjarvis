# Normalize line endings to LF on checkout across all platforms so that
# Prettier (endOfLine: lf) and tests behave identically on Windows CI.
* text=auto eol=lf

# Binary files Git should never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
