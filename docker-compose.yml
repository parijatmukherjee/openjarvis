# Local reproducible test environment.
#
# Run the full CI gate in a clean Linux container (mirrors what gates PRs):
#
#   docker compose run --rm test
#
services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: openjarvis-test
